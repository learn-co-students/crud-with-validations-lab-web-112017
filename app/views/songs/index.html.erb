<table>
  <tbody>
    <% @songs.each do |song| %>
      <tr>
        <td><%= link_to song.title, song_path(song) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to "Add New Song", new_song_path %>
